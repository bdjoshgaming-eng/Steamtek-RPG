STEAMTEK RPG - CLAUDE HANDOFF
Updated: 2026-07-23, Live3D combat deep dive (grenade/flame/dodge/
         buttstroke, Phase 6 Batch 2 mods, Phase 8 repair/dismantle/
         rebuild, weapon Speed/Ammo/Reload/stamina rework, player
         chat bubbles) -- read section "SESSION 2026-07-23b" first,
         then the 2D-deletion session below it. The 2D main.tscn/
         main.gd system, described in the "PRE-EXISTING HANDOFF"
         section far below, IS FULLY DELETED from the working tree.
=============================================================

-------------------------------------------------------------
SESSION 2026-07-23b -- LIVE3D COMBAT DEEP DIVE (grenades, flame,
                        mods live, durability/repair, weapon economy)
-------------------------------------------------------------
Full detail: docs\claude handoffs\
  2026-07-23_Live3D_Combat_Deep_Dive.md

SCOPE. Direct continuation of the "2026-07-23" session below (same
day). Closed out the entire ranged-combat-redesign cluster (Grenade
Launcher telegraph+physics+collision-damage, Flame Thrower channel,
Grit DoT/CC, Charged Shot, Buttstroke, Dodge Roll -- all now fully
built and confirmed working), then kept going into crafting Phase 6
Batch 2 (mods finally affect combat, not just data), crafting Phase 8
(durability/repair/dismantle/rebuild), and a full weapon resource-
economy rework. Read the detail doc -- this is a LOT, and several
things (grenade damage model, grenade burst-fire, grenade accuracy
scatter) were built, tested, and explicitly REVERTED within this same
session based on live feedback, so skimming only the current handoff
top risks re-proposing something Josh already said no to.

WHAT SHIPPED (one line each, see detail doc for the real substance)
- Grenade Launcher: real ballistic physics (solved launch velocity,
  gravity+bounce), damage is DIRECT HIT via continuous contact
  detection during the whole flight (not a check at the final rest
  spot -- that was the reliability bug), plus a secondary splash layer
  for anyone else caught nearby. Splash radius is explicitly reserved
  for future Rocket Launcher/Arc Cannon, NOT grenades.
- Flame Thrower: hold-to-channel cone, heat/overheat resource, HUD
  heat bar (equipped-weapon-conditional).
- Grit's DoT/CC: ScrapThief's new "Corrosive Grasp" special is the
  first real consumer of Combat.gd's grit_dot_reduction()/
  grit_cc_duration_mult(), which existed but had nothing to resist
  before this session.
- Charged Shot, Buttstroke, Dodge Roll all fully built and confirmed.
- Phase 6 Batch 2: mods now actually change combat stats/damage type,
  not just sit in data. Install/remove UI in the inventory window.
- Phase 8: durability actually drains from combat now, repair/
  dismantle/rebuild UI wired to real CraftingService functions.
- WEAPON SPEED/AMMO/RELOAD/STAMINA REWORK (the biggest structural
  change): Speed is now a real attack cooldown, Ammo Capacity/Reload
  Speed drive a real magazine+reload system, and Action was fully
  repurposed into a stamina pool for sprint/dodge -- weapons/abilities
  NEVER spend Action anymore, only ammo/heat/durability/cooldowns.
- Player chat bubbles (WoW/FFXIV-style, blue) separated from quest
  item dialogue (the modal SteamtekDialogueBox) -- character.say()/
  think() for speech vs. inner monologue.
- Universal weapon-equip bug fixed (was apartment-only).
- Weather F4 debug toggle.
- Standing color rule set: player telegraphs blue/green
  (damage/heal), enemy telegraphs red/yellow (damage/CC).

STILL ON HOLD, DO NOT BUILD WITHOUT ASKING FIRST
- Steamtek Assembly System -- its socket/grade curves are keyed on
  "floor depth" from the deleted 2D 60-floor system, which has no
  equivalent anywhere in the Live3D game. Confirmed via grep: nothing
  in scenes/ calls the campaign generator. Needs a real zone/depth
  concept to exist first.
- Crafting Phase 7 (15-node pick-4 crafting keystone) -- the keystone
  system itself is expected to be reworked.

See the detail doc for exact numbers, file-by-file changes, and the
full "tried and reverted" history (grenade burst-fire, grenade
accuracy scatter) so you don't re-propose either.


-------------------------------------------------------------
SESSION 2026-07-23 -- 2D DELETED, 3D COMBAT + INVENTORY BUILT,
                       PERSISTENCE REMOVED
-------------------------------------------------------------
Full detail: docs\claude handoffs\
  2026-07-23_3D_Pivot_Combat_And_Persistence_Removal.md

SCOPE. The 2D prototype (scenes/main.gd, main.tscn, player.gd,
enemy.tscn, and every sibling 2D-only UI script) was permanently
deleted this session at Josh's explicit request ("I want the 2d
prototype scene gone, obliterated. no trace left. 3d is the way
forward"). Commit 39846d2. Everything in the "PRE-EXISTING HANDOFF"
section below this point describes that now-deleted system --
treat it as historical/git-archaeology reference only, recoverable
from commit 31122e5, NOT live code. GameData.gd/Combat.gd/
CombatData.gd/KeystoneViewer.gd/systems/crafting/*.gd survived --
those are shared with, and now exclusively used by, the 3D pipeline.

WHAT SHIPPED
- Full 2D prototype deletion (see file manifest in the detail doc).
- A global inventory system: items/weapons/cogs/equipped-weapon now
  shared across every Live3D scene (apartment/lantern/surface)
  instead of each scene keeping its own disconnected copy, and the
  [I] inventory panel now works in every scene, not just the
  apartment. Fixed by extending the SteamtekLive3DProgressStore
  autoload (get_global_inventory/save_global_inventory), NOT by
  adding a new autoload.
- HARD LESSON: an earlier attempt added two NEW autoload singletons
  (SteamtekPlayerCombatState, SteamtekPlayerInventory) and hit a
  Godot 4.7 editor UID-cache desync bug ("Failed to create an
  autoload, can't load from UID or path") that blocked the game from
  launching at all, and that a full "Reload Current Project" did NOT
  fix. STANDING RULE: never register a new autoload singleton for
  shared runtime state -- always extend SteamtekLive3DProgressStore
  with a new key/method instead.
- Basic 3D ranged combat: mouse-aim + left-click hitscan attack,
  shared player health/action pool (same safe extend-the-store
  pattern), enemy chase/attack/receive-damage loop, confirmed working
  end-to-end in-game (fired on the ScrapThief, its health dropped).
  Scope is intentionally basic -- no telegraphs/AoE for Grenade
  Launcher or Flame Thrower yet, no Grit/Charged Shot/Dodge Roll
  (those are still 2D-only design intent, not rebuilt in 3D).
- Persistence removed ENTIRELY per Josh's explicit request ("please
  delete the persistant game code. Ill just start a new game every
  time.") -- SteamtekLive3DProgressStore no longer touches
  FileAccess/user:// at all, it's a pure in-memory Dictionary cache
  for the lifetime of the running process. Cross-scene consistency
  within ONE session still works (autoloads survive scene changes on
  their own); a fresh Play always starts completely clean now.

See the detail doc for exact code-level specifics on every item above.


-------------------------------------------------------------
SESSION 2026-07-22 -- LIVE3D APARTMENT + REUSABLE UI FRAMEWORK
-------------------------------------------------------------
Full verbatim transcript: docs\claude handoffs\
  2026-07-22_Live3D_Apartment_UI_Session_Transcript.txt

SCOPE. This session is entirely inside the Live3D pipeline
(scenes\levels\..., scenes\gameplay\live3d\...), separate from
the 2D main.tscn/main.gd crafting+combat system documented below.
Nothing in this section changes anything in the CRAFTING SYSTEM or
COMBAT sections further down this file.

WHAT SHIPPED
- Deleted ALL remaining legacy Playable3D content: the old opening
  apartment, the old "Rain Alley" surface tutorial scene + district,
  and a stray test scene. Confirmed via repo-wide grep that nothing
  still references the old names.
- Rebuilt the opening apartment from scratch at
  scenes\levels\apartment_3d\SteamtekApartment.tscn +
  steamtek_apartment.gd, reusing the proven kit art but with a
  redesigned room layout (only the two "far" walls built -- the
  camera looks in from the south-east octant, so building all four
  walls blocks the view; this was the root cause of an early "can't
  see the room" bug).
- Attempted a "Rain Alley" full rebuild (scenes\levels\surface_3d\
  SteamtekRainAlley.tscn + district), but Josh rejected it for
  reusing too much of the old scene's shape/feel despite fresh
  names. DELETED. Replaced with a genuinely blank placeholder,
  scenes\levels\surface_3d\SteamtekSurfaceBlank3D.tscn (empty floor
  + one door back to the apartment + spawn marker), which Josh is
  now hand-building in the Godot editor with the Steamtek Live3D
  Builder dock. DO NOT regenerate or overwrite this file's content
  wholesale -- it is actively being edited by hand.
- Main menu: scenes\menus\SteamtekMainMenu.tscn +
  steamtek_main_menu.gd, minimal placeholder (title + Play button),
  set as project.godot's run/main_scene. Play -> SteamtekApartment.

BUILT A REUSABLE LIVE3D UI FRAMEWORK (scenes\gameplay\live3d\)
Josh asked to pause content work and get the UI layer itself
settled first, since it will be reused for every future area:
  - steamtek_item_icon_registry.gd -- placeholder-icon lookup
    (category color + short abbreviation), no art assets needed.
    Real icon art later means editing ONLY this file.
  - SteamtekItemIcon.tscn/.gd -- one colored square + abbreviation,
    used everywhere below.
  - SteamtekItemSlot.tscn/.gd (extends Button) -- icon + count badge
    + native tooltip + double_clicked signal. NAMED icon_display,
    NOT "icon" -- Button already has a native "icon" export property,
    and reusing that name silently breaks script attachment for the
    whole scene (see INCIDENTS below).
  - SteamtekItemGridPanel.tscn/.gd -- generic titled grid of slots,
    used for the storage crate's two panels.
  - SteamtekInventoryWindow.tscn/.gd -- the full player inventory:
    FIXED 48-slot grid (6x8, not resized per item), a capacity
    readout, Inventory/Mission Items/Currency tabs, and a real
    "Details" side panel (mirrors main.tscn's own Details|Items
    inventory-book layout) that shows quantity plus, for weapons,
    REAL base stats and computed DPS pulled straight from
    GameData.ITEM_DEFINITIONS[item].weapon_stat_ranges[stat][0] --
    same numbers main.tscn's own inventory would show for that item
    at quality 0. Styled with the project's existing dark/amber
    palette, not the SWTOR reference screenshot's blue.
  - steamtek_live3d_hud.gd/.tscn -- persistent health/action bars +
    8 action slots (cosmetic only, no combat loop in these scenes
    yet), Talents panel, Crafting panel.
  - TALENTS PANEL REUSES THE REAL KeystoneViewer.gd (scenes\
    KeystoneViewer.gd) AS-IS, unmodified, via a tiny local stub
    (steamtek_local_talent_bridge.gd) standing in for main.gd's
    `main` field (xp_pools, professions_unlocked, _show_combat_
    message). GameData.novice_professions is a real shared autoload,
    so this is genuinely live, shared talent-tree state, not a fake
    copy -- reload main.tscn later and the same keystones show up
    unlocked.
  - CRAFTING PANEL is a SIMPLIFIED LOCAL implementation, not a reuse
    of the real CraftingPanel.gd (that one is too deeply coupled to
    main.gd's material_batches/_perform_craft/CraftingService to
    reuse cheaply). Real recipe data from CraftingData.BLUEPRINTS,
    real material family display names from CraftingData.
    RESOURCE_FAMILIES(+_EXTRA), checked against the SCENE'S OWN
    local `progress["items"]` dictionary. No quality/experimentation
    simulation -- just "do you have the materials, craft the item."
  - SteamtekDialogueBox.tscn/.gd -- generic NPC "chat box" (speaker
    + portrait-placeholder + body text + a Callable-driven option
    button list), modeled on the UX of scenes\TrainerDialogue.gd and
    scenes\Quest.gd's popup (good reference, NOT reused directly --
    both are hardwired to main.gd) but written data-driven. Currently
    used for the apartment note's inner-monologue sequence (was a
    bottom-of-screen toast, now a real popup with Continue/Close).
    No NPC exists yet in any Live3D scene -- this is built and wired
    for the first one that does.
  - SteamtekQuestBubble3D.tscn/.gd -- floating TEXT speech bubble
    (rounded panel + a small pointer-tail triangle) that projects a
    3D marker's position to screen space and shows/hides by distance
    to the camera. Pairs with, does NOT replace, the existing
    icon-only "!" SteamtekQuestMarker3D.

APARTMENT PROGRESSION -- UN-GATED FROM "EMPTY THE CRATE"
Originally the exit door required note_found AND storage_collected
(crate fully emptied) AND equipment_complete. Josh flagged this as
forced/tutorial-feeling -- a real storage container should let you
take some things, leave others, and come back later. Fixed:
  - Exit door now only requires note_found + equipment_complete.
  - Storage window shows TWO panels: crate contents (take, double-
    click) and "your items" (store back into the crate, double-
    click) -- genuinely two-way now.
  - Hold [R] for ~0.6s while the crate is open takes everything.
  - Storing the currently-equipped weapon back into the crate
    correctly un-equips it and re-locks the door.

STARTING KIT stays sourced from the REAL numbers in main.gd
(_grant_profession_starting_kit / _grant_starting_bandages): Crate
of Bandages x2 (5 charges), Mineral Survey Tool, Rusty Crafting
Kit, Riveted Knuckles, Rusty Pistol, 100 Cogs. NOTE main_handoff.txt
below lists Mineral Survey Tool / Rusty Crafting Kit as ORPHANED,
ITEM_DEFINITIONS-removed dead items in the 2D game -- they are
alive and real in this Live3D economy regardless; if the 2D
ITEM_DEFINITIONS entries are ever restored, keep both paths in
sync.

INCIDENTS THIS SESSION
- SteamtekItemSlot named its icon field `icon`. Button already has
  a native `icon` export (Texture2D). GDScript refuses to redefine
  a native member, so the WHOLE SCRIPT silently failed to parse,
  which cascaded into "Cannot call method on a null value" errors
  several call-sites away and a completely black screen with no
  obvious cause. Renamed to `icon_display`. LESSON: before naming a
  Button/Control subclass field something generic (icon, text,
  value, pressed...), check it isn't already a native property --
  the failure mode is silent and the resulting errors point
  everywhere except the real cause.
- A blueprint-row icon called set_item() one line before its parent
  HBoxContainer was added to the actual scene tree, so its own
  @onready children (background/label) were still null.
  LESSON: add_child() a node into the ALREADY-in-tree hierarchy
  before calling anything on it that depends on that node's own
  @onready vars -- _ready() only fires once a node is reachable from
  the root, not the moment add_child() is called on a detached
  parent.
- Closing the storage crate with [E] sometimes silently reopened it.
  Root cause: steamtek_humanoid_character_3d.gd's _physics_process
  polls Input.is_action_just_pressed("interact") INDEPENDENTLY of
  this scene's _unhandled_input. Re-enabling player_controlled in
  the same frame the close came from let the SAME keypress read as
  fresh on the very next physics tick, instantly retriggering
  attempt_interaction(). A `call_deferred` fix reduced but did not
  eliminate it (frame-timing dependent). The real fix: a
  `_suppress_storage_reopen` flag set on close, only cleared once
  `Input.is_action_pressed("interact")` reads false (key physically
  released) -- deterministic regardless of frame timing.
  LESSON: two independent systems polling the same input action
  each frame is a race condition waiting to happen; block on actual
  key-release state, not on hoping the timing works out.
- ExitDoor's position was eyeballed instead of using the door wall
  module's own authored `DoorInteraction` Marker3D socket (APT_Wall_
  Door_300x270_Service.tscn, local offset (0, 1.1, 0.22)). Confirmed
  visually with a temporary bright debug-sphere child before/after
  the fix -- worth doing again any time a trigger position is in
  doubt, much faster than guessing from screenshots alone.
  LESSON: check for an authored placement socket before hand-
  guessing a world position next to a modular kit piece.
  SteamtekZoneDoor3D.tscn's shared interaction box (used by EVERY
  zone door in the game) was also enlarged then re-shrunk this
  session while chasing the above -- it currently sits at
  Vector3(1.4, 2.0, 1.0), tight/close-range on purpose per Josh's
  explicit request ("I want to be standing right on top of the
  door"). Do not enlarge this again without asking first.

OPEN / NOT YET DONE
- No NPC exists anywhere in the Live3D pipeline yet -- SteamtekDialogueBox
  is built and proven (via the note) but has never driven an actual
  conversation with branching options.
- Crafting panel's material-slot icon shows the BEST-carried accepted
  family only; a slot whose "accepts" list has two families the
  player holds SOME of each of will not combine them (matches how the
  real CraftingPanel.gd's slot-selection UX works, but is worth
  double-checking if that assumption ever changes).
- Blank canvas scene (SteamtekSurfaceBlank3D.tscn) has no HUD/
  inventory/crafting wired in yet -- deliberately left for Josh to
  finish placing geometry in first.
- Storage window's own crate does NOT lock/unlock player movement
  the same way the old apartment's inventory window used to (Josh
  asked for inventory specifically to stop locking movement; storage
  crate still does, untouched, not requested).

=============================================================
BELOW THIS LINE: PRE-EXISTING HANDOFF -- 2D PROTOTYPE, DELETED
2026-07-23 (commit 39846d2). NONE OF THIS CODE IS LIVE ANYMORE.
main.gd, main.tscn, player.gd, enemy.tscn, and every 2D-only UI
script (CraftingPanel.gd, InventoryBook.gd, AbilityBook.gd, etc.)
described below no longer exist in the working tree. Kept here
purely as historical/design reference -- the exact numbers and
mechanics are still recoverable from commit 31122e5 if ever worth
rebuilding against the 3D character/scene architecture instead.
Do not propose editing any file named below as if it still exists.
=============================================================

>> See docs/claude handoffs/STEAMTEK_WORKFLOW.md for the session
   workflow (batch fixes with FIXES:, stage features with FEATURE:).

-------------------------------------------------------------
PROJECT BASICS
-------------------------------------------------------------
- Steamtek RPG: co-op action RPG / roguelite. Godot 4, GDScript.
- Repo: bdjoshgaming-eng/Steamtek-RPG
- Local path: C:\My Game\Steamtek-RPG
- Two machines (work + home). INSTALL DELIVERED FILES ON BOTH THE SAME
  DAY -- a stale GameData.gd once silently disabled a whole combat
  phase for several sessions.

DIRECTORY LAYOUT
  systems\crafting\   crafting_data.gd, crafting_models.gd,
                      crafting_service.gd, crafting_resource_generator.gd
  scenes\             main.gd, CraftingPanel.gd, KeystoneViewer.gd,
                      GameData.gd, CombatData.gd, Combat.gd
  docs\claude handoffs\

  The crafting files use `class_name`, so no autoload registration.
  GameData/CombatData/Combat live in scenes\ and are registered as
  autoloads -- do NOT move them without updating Project Settings.

-------------------------------------------------------------
HARD RULES
-------------------------------------------------------------
- .gd files: ASCII ONLY. Non-ASCII causes SILENT parse failures.
- TABS, never spaces. CRLF line endings.
- COMPLETE drop-in files, never diffs. Josh does not hand-edit.
- INSTALL BY REPLACING THE FILE IN FILE EXPLORER, not by pasting into
  the Godot script editor -- see INCIDENTS #5.
- Multi-file deliveries are a MATCHED SET. A "function not found in
  base CraftingData/CraftingService" error means a file did not get
  copied, not a code bug.
- Before every export: ASCII check, tab check, undeclared-identifier
  sweep, and for large refactors a DECLARATION COUNT comparison.

-------------------------------------------------------------
CURRENT STATE
-------------------------------------------------------------
Combat is complete through Phase 10 and stable. The old crafting and
surveying systems were deleted entirely. The new crafting system is
built to spec Phase 6 and wired into the game: scavenge -> materials
with provenance -> blueprint -> experimentation -> sockets -> a real
item with real stats -> permanently fitted mods.

Combat Phase 6c is also done: enemies are INSTANTIATED from Enemy.tscn
at runtime from a spawn table, not hand-placed in main.tscn. Adding an
enemy is now a table entry.

-------------------------------------------------------------
COMBAT (unchanged, complete through P10)
-------------------------------------------------------------
Autoloads: Combat.gd (math), CombatData.gd (data).

  Damage = WeaponDamage x AbilityCoefficient x ClassModifier
         x CertificationModifier x CriticalModifier x WeakPointModifier
  (ClassModifier and WeakPointModifier are 1.0 placeholders)

  Mitigation (armor AND resistances): Rating / (Rating + 140)
  Damage types (7): Kinetic, Ballistic, Thermal, Pressure, Arc,
    Chemical, EMP. Chemical and EMP have NO weapons -- inert.
  Hidden Combat Level 1-100 drives health, action, damage, defense,
    armor, dodge, block, crit_resist.

  Rank marks (CL never shown as a number):
    /  1-5    //  6-10   ///  11-15   ////  16-20
    <  21-30  <<  31-40  <<<  41-55   <<<<  56-70
    *  71-84  **  85-94  ***  95-100

STARTUP DATA VALIDATION
  Combat.validate_game_data(), called from main._run_startup_validation().
  Seven checks guarding the .get(key, default) silent-failure shape.
  Real problems -> push_error + red dismissible banner (click or ESC).

-------------------------------------------------------------
CRAFTING SYSTEM
-------------------------------------------------------------
DESIGN DECISIONS
  - Plain Dictionaries, NOT Resource/.tres. Save file is JSON, so they
    round-trip with no conversion layer.
  - Material Quality is 0-100. The legacy weapon "Quality" is 0-1000.
    QUALITY SCALE BRIDGE in _grant_crafted_item multiplies by 10. Do
    not "simplify" that away or every crafted weapon sits at the bottom
    of its stat ranges.
  - 4-of-15 keystone selection is CRAFTING ONLY. Combat keystones keep
    the point-budget model.
  - The crafting keystone gets its OWN panel, separate from the talent
    viewer.
  - Every crafting keystone node needs a DOWNSIDE (Material Insight and
    Precision Calibration as specced are pure upside = auto-picks).

PHASE 1 - DATA FOUNDATION (done)
  Material batches, crafted item instances, player profile, all with
  factories and to_dict/from_dict. craft_seed stored from day one.
  Provenance (source_id / floor_id / location_id) on every batch.

PHASE 2 - CAMPAIGN RESOURCE GENERATOR (done)
  60 floors: Survivors 1-20 (quality bias -8), Expansion 21-40 (0),
  Builders 41-60 (+12). Deeper is OLDER but cleaner. 15 floor
  identities, 22 resource families. Same seed -> identical map;
  reload never rerolls; guarantees always satisfiable via deterministic
  repair. NOTE: guarantees are met naturally on only ~33% of seeds;
  the rest get repaired. Placeholder numbers, needs playtesting.

PHASE 3 - BASIC CRAFTING (done, wired in)
  Alley dumpster yields real MaterialBatches. Blueprint panel with
  material slots. Medicine blueprints (bp_bandages, bp_antiseptic_salve)
  restored medicine after the rip-out.

PHASE 4 - EXPERIMENTATION (done, wired in)
  8 categories (Output, Precision, Efficiency, Thermal Control,
  Durability, Handling, Potency, Mod Architecture), each mapped to a
  stat and to the material traits that help it.

  Point generation: Base 3 + familiarity 0-3 + materials 0-2
  + workshop 0-2 (Phase 8) + tools 0-2 (Phase 8) + keystone 0-3
  (Phase 7). Workshop/tool/keystone all contribute 0 today, so the
  ceiling rises automatically when those land.

  Result tiers: Breakthrough 95+ (x1.6), Exceptional 82 (x1.3),
  Great 68 (x1.0), Good 52 (x0.75), Standard 35 (x0.5), Poor 18
  (x0.25), Failure below (x0.0).

  Risk modes: Stable (+5 score, +/-8 spread, 0% flaw chance),
  Standard (0, +/-15, 25%), Aggressive (+12, +/-30, 55%).

  FAILURE COSTS THREE THINGS (Josh's call): points wasted, a
  risk-scaled chance of a permanent instability on the item, and a
  socket_chance_modifier penalty read by Phase 5. Failed categories
  also cost 8 durability each.

  ANTI-SAVE-SCUM: craft_seed is DERIVED from blueprint + sorted material
  batch ids, NOT randomised. Reload + same materials + same allocation
  + same risk = identical result. Allocating DIFFERENTLY gives a
  different result, which is correct -- that is a choice, not a reroll.

  TUNING NOTE: concentrating points in one category yields more total
  benefit than spreading them. Real choice, but spreading is currently
  the weaker play rather than a genuine alternative.

PHASE 5 - MOD SOCKETS (done, wired in)
  Opportunity score feeds four probability bands:
    low <25       90/10
    moderate 25-49  55/40/5
    strong 50-74    20/60/18/2
    exceptional 75+  5/45/40/10
  Mod Architecture allocation is the dominant term (x12 per point),
  plus tier multiplier x10, familiarity, material compatibility,
  workshop; minus difficulty x0.20 and 5 per instability, plus the
  Phase 4 failure penalty. Rolls derive from craft_seed via an
  independent derivation, so reopening the UI cannot reroll.
  Consumables declare max_sockets 0 and are skipped.

PHASE 6 - MODS (LOGIC BUILT, NOT WIRED IN)
  BATCH 1 SHIPPED: 8 weapon mods, 5 grades, install rules, stat
  effects. Scope is WEAPON mods only -- the spec also lists Armor and
  Cybernetics but Steamtek has no such item types yet.

  Grades: Standard 1.0, Refined 1.4, Advanced 1.8, Prototype 2.3
  (drawback x1.6), Masterwork 2.1 (drawback x0.0). Masterwork gives
  LESS raw power than Prototype but removes the penalty, so the top
  grade is a choice not a strict upgrade.

  Socket tags gate meaningfully:
    Piston Blade (Kinetic/Utility): Balanced Haft, Pressure Chamber,
      Recoil Stabilizer, Reinforced Binding, Targeting Optic
    Arc Rod (Arc/Thermal/Utility): Conductive Accelerator, Heat Sink,
      Status Module, Reinforced Binding, Targeting Optic
  Incompatibility tags stop stacking (no two damage cores).

  BATCH 2 STILL TO DO: main.gd integration (mods affecting equipped
  stats, granting mods), and the install/remove UI.

REMAINING SPEC PHASES
  7 crafting keystone (15-node choose-4, own panel, needs downsides)
  8 repair / dismantle / rebuild

-------------------------------------------------------------
KEY INTEGRATION POINTS IN main.gd
-------------------------------------------------------------
_realise_item_stats(item_key, definition, quality)
  SHARED by _grant_starting_weapon and _grant_crafted_item. Reads
  weapon_stat_ranges and scales each stat by quality, computes DPS.
  Extracted because crafted weapons were previously granted with ONLY
  a Quality value -- no damage, speed, accuracy or damage type. They
  were blank items dealing the fallback 5 damage. Keep both grant
  paths going through this so they cannot drift again.

crafted_item_instance_of  inventory item_key -> crafted instance_id.
  Lets the inventory detail panel show traits, flaws, socket types and
  durability. Persisted in save/load.

_scavenge_dumpster()  draws from EVERY live surface source, not just
  the ones located at "Alley Dumpster". Copper (Stripped Wiring) and
  black iron (Rusted Railing) exist in SURFACE_SOURCES but have no
  physical props, so they were unreachable. When props are added,
  split them back into separate interactables.

-------------------------------------------------------------
KNOWN DEBTS AND LIMITATIONS
-------------------------------------------------------------
TESTING VALUES LEFT IN (restore before any balance pass)
  - DUMPSTER_RESPAWN_TIME = 5.0, was 45.0. At 5s the surface salvage
    economy is meaningless. Josh asked to leave it for now.

CONSUMABLES DO NOT SCALE WITH QUALITY
  Bandages heal a flat BANDAGE_HEAL_AMOUNT (100) regardless of craft
  quality, and the heal constant is used directly at the point of use.
  So Efficiency and Potency experimentation do NOTHING on the item
  players craft most. Blood Bag already scales via _scale_by_quality --
  the machinery exists, bandages just do not use it. Fix: give bandages
  a min/max heal range and have Efficiency reduce BANDAGE_ACTION_COST.

BATCH POOLING
  A material slot must be filled from ONE batch (_batches_for_slot
  requires a single batch with amount >= needed). Batches only merge
  when family, quality, trait, instability AND source all match. In the
  Silo, where many sources of the same family exist, a player will be
  able to carry plenty of a material and still not fill a slot. The
  panel now says "not enough in one batch -- N held across all
  batches" so it is at least honest. Options: leave it (one lot per
  slot is defensible), allow pooling with averaged quality (preferred,
  needs multi-select UI), or merge more aggressively (destroys
  provenance, which the spec wants).

SURFACE MATERIALS HAD NO TRAITS (fixed, but see below)
  generate_surface_sources() hardcoded primary_trait_id "", so every
  surface material was traitless and EVERY experimentation category
  read "materials do not help here" for the whole early game. Fixed to
  pull from the family's eligible_traits.
  BUT the campaign map is generated ONCE and saved whole -- that is the
  anti-reroll guarantee -- so an EXISTING SAVE keeps its traitless
  sources forever. A new campaign is needed, or a one-time migration
  that backfills traits from the campaign seed.
  GENERAL RULE: fixes to the resource generator NEVER affect an
  existing campaign.

TRAIT/CATEGORY PAIRING IS VIA TRAIT_CATEGORY_AFFINITY
  NOT via the trait "influences" / category "affects" strings -- those
  are FLAVOUR TEXT. Claude misdiagnosed this once and edited data on
  the wrong theory. TRAIT_CATEGORY_AFFINITY is complete and correct.

MATERIAL COMPATIBILITY HINT IS A WHOLE-RECIPE RATIO
  material_compatibility scores against the weight of EVERY slot, so a
  part-filled selection can never score well (Piston Blade's binding is
  0.15 of the recipe). The hint now only shows once the selection is
  COMPLETE. Do not "fix" this by dividing by filled slots only.

LOOT STILL DROPS INERT MATERIALS
  Loot tables drop Copper/Iron etc. from the old system. Those items
  have no consumer now. Data debt, cheap to fix once the new material
  model is settled.

ORPHANED ITEMS IN OLD SAVES
  Mineral Survey Tool, Rusty Crafting Kit etc. remain in inventories
  from before the rip-out. Their ITEM_DEFINITIONS were removed so they
  do nothing.

-------------------------------------------------------------
OPEN DECISIONS
-------------------------------------------------------------
- SCAVENGING XP: Josh is working out a separate XP track for it.
  Crafting XP is deliberately PARKED -- it accrues with no sink and
  that is fine for now. Do not build an XP economy around it yet.
- CL 41-100 anchors undefined (table only covers CL 1-40).
- Does killing a CL30 enemy pay more Combat XP than a CL5 one? Kill XP
  is currently a flat per-enemy constant ignoring CL.
- Player crit is DEAD: conditioning_nodes is a legacy path-based hook
  that returns 0 under keystones.
- Auxiliary's 12 points cover both combat and future crafting nodes.

-------------------------------------------------------------
COMBAT PHASE 6c - RUNTIME ENEMY SPAWNING (DONE)
-------------------------------------------------------------
Enemies are no longer hand-placed nodes. Enemy.tscn is instantiated
per spawn-table entry at startup.

  scenes\enemy.tscn   (path is HARDCODED as res://scenes/enemy.tscn)
    Enemy (Node2D)
    +-- Visual (Sprite2D)
    +-- HealthBarBg / HealthBarFill (Polygon2D)
    +-- ActionBarBg / ActionBarFill (Polygon2D)
    +-- NameLabel (Label)
    +-- TargetIndicator (Line2D)
    +-- AttackCooldownTimer (Timer)
    +-- RespawnTimer (Timer)

ENEMY_SPAWN_TABLE in main.gd holds display_name, cl, archetype,
faction, position, sprite_scale, tint, kill_xp, loot_key,
attack_cooldown, respawn_time. ADDING AN ENEMY IS A TABLE ENTRY.

Instances are parented to World/YSortLayer/Enemies. DO NOT DELETE the
Enemies node -- it is the spawn parent. It should now be EMPTY.

BARS ARE CHILDREN of each enemy, so they follow it automatically. All
the old per-frame global repositioning in _update_health_bars is gone.

Removed: 18 declarations (the dummy_*/enemy2_* @onready nodes, both
target-indicator vars) and the four enemy timer signal connections,
which are now made in _spawn_enemy.

Rewritten to iterate enemy_nodes: _update_health_bars,
_update_enemy_hud, _setup_health_bars, _cycle_target,
_try_click_target, _get_tracked_enemy_id. Zero hardcoded enemy refs
remain.

STILL AT CL1/CL5, so armor, dodge, block, crit_resist and rank-mark
differences remain INVISIBLE in testing. Adding a CL20 entry to the
spawn table is now a one-minute change and is the fastest way to see
those systems work.

_make_target_indicator() is orphaned (no callers) -- cleanup.

-------------------------------------------------------------
STEAMTEK ASSEMBLY SYSTEM (specced, NOT built)
-------------------------------------------------------------
Full design in docs\claude handoffs\STEAMTEK_ASSEMBLY_SYSTEM.md.
The rare-item path, modelled on Neocron's Tech Parts.

CORE RULE: Steamtek gives +1 OVER WHAT YOU COULD OTHERWISE MAKE AT
THIS DEPTH. Applies to sockets AND to mod grade. That is why it stays
relevant from floor 1 to 60 instead of being an endgame bolt-on.

Steamtek is an INGREDIENT TIER, not a mod grade. Parts feed blueprints
across categories: X parts make a mod, or a weapon, or (later) armor.

Fragment tiers, mirroring Neocron's T/E/L:
  Salvaged  surface, floors 1-20, early bosses
  Intact    floors 21-45
  Pristine  floors 46-60, exceptional bosses, quests
A fragment's tier SCALES ITS RESULT -- Salvaged parts in a low-CL craft
give a good LOW-CL item, not an endgame one.

Fragments drop UNIDENTIFIED. Analysis at a bench reveals what they are.
NOTHING FAILS -- no destroyed parts on Analysis or Assembly. The grind
is the entire cost. (Neocron destroyed parts; rejected.)

Socket ceiling is TWO TUNABLES AND A CURVE, never a hardcoded count:
MAX_SOCKETS (currently 5), a depth ceiling table, and Steamtek = depth
ceiling + 1. Normal crafting tops out at MAX_SOCKETS - 1, so the top
socket is permanently Steamtek-exclusive at any setting.

BLOCKERS
  - No boss enemies and no AI, so "exceptional boss" drops cannot
    exist. Early Steamtek must come from floor loot and quests first.
  - Needs a minimal Analysis Bench (workbenches are spec Phase 8).
  - Blueprint max_sockets must become DERIVED from depth, or it
    silently overrides the curve.
  - Phase 5 opportunity bands must roll WITHIN the depth ceiling.

OPEN: confirm "Artifact" as the top mod grade name; fragments per
schematic per tier; whether schematics drop or are researched.

-------------------------------------------------------------
OTHER OPEN WORK
-------------------------------------------------------------
COMBAT PHASE 6c - runtime enemy spawning. Needs Godot EDITOR work from
  Josh (Enemy.tscn, dynamic health bars / name labels / indicators).
  Kills the last 26 hardcoded refs in main.gd.

COMBAT PHASE 8 - enemy AI. Enemies have NO movement or AI at all; they
  are stationary and attack only when the player walks into
  ENEMY_ATTACK_RANGE. This is BUILD AI, then tier it by CL.
  Multi-session, and the change that most affects how the game FEELS.

CLEANUP
  - KeystoneViewer.gd has a dead "Crafting" entry in COLORS and a
    hardcoded CENTER = Vector2(960, 540).
  - Legacy skill_xp_thresholds / skill_point_costs / skill_cogs_costs
    arrays may be dead.
  - ACTION_BAR_BOTTOM_MARGIN = 70 for embedded testing; drop to 20-30
    for release.

-------------------------------------------------------------
INCIDENTS AND LESSONS
-------------------------------------------------------------
1. GAMEDATA DESYNC. A stale GameData.gd on both machines carried
   pre-4b damage type names. Twelve weapons matched no resistance key,
   got rating 0, and bypassed enemy armor entirely for several sessions
   while the system was being built on top of. Failed silently because
   a missing key returns a legal zero.
   LESSON: install on both machines the same day. The startup guard now
   catches this class of bug, but only AFTER the fact.

2. FUNCTION-SPAN DELETION (happened TWICE). A bulk edit defined a
   function's end as the NEXT `func` keyword, swallowing any top-level
   declaration block between them. Once ~725 lines and 30+ functions;
   once 80 declarations including BASE_HIT_CHANCE, MELEE_RANGE,
   inventory, equipped_weapon_name. Both recovered from a pre-edit
   backup.
   LESSON: a function ends at the first COLUMN-0 non-blank line. Back
   up before bulk edits. Compare DECLARATION COUNT before and after --
   that is the check that catches it immediately. Dry-run large cuts.

3. LOST DECLARATION AFTER RECOVERY. Restoring from a snapshot rolled
   back a `var enemy_nodes` declaration while the redo rebuilt only the
   functions USING it -- 9 "not declared in current scope" errors.
   LESSON: after recovering, re-check that every declaration the
   refactor introduced still exists.

4. RIGHT_X ORPHAN. A refactor REMOVED a declaration while two child
   controls still referenced it.
   LESSON: grep for users before deleting a declaration.

5. PASTE-INSTEAD-OF-REPLACE. main.gd ended up containing the new file
   followed immediately by the old one ("...visible = falseextends
   Node2D"), producing 447 errors. The delivered file was clean; the
   corruption happened at install.
   LESSON: install by REPLACING THE FILE in File Explorer. Do not paste
   contents into the Godot script editor -- the paste lands at the
   caret rather than overwriting.

6. UI BUILT WITH ABSOLUTE OFFSETS. The crafting panel was positioned
   with pixel offsets measured up from the bottom of the screen, then
   with full-screen containers. The first broke at any viewport height
   other than the one assumed; the second smeared sparse content across
   1080px. Third attempt -- a FIXED-SIZE CENTRED window (1180x764) --
   works.
   LESSON: for code-built panels, use a fixed-size centred frame with
   containers inside. There is a comment in CraftingPanel._build_ui
   recording this so it does not get "fixed" back.

7. LABEL TOOLTIPS SILENTLY DO NOTHING. Godot Labels default to
   MOUSE_FILTER_IGNORE, so they never receive hover and tooltip_text is
   never shown. Category tooltips existed for a whole phase without
   ever appearing.
   LESSON: set mouse_filter = MOUSE_FILTER_STOP on any Label carrying a
   tooltip.
8. SWEEPING FOR REMOVED IDENTIFIERS -- CHECK BARE NAMES TOO.
   After Phase 6c removed the dummy/enemy2 node vars, two lines still
   read `dummy if target_id == "dummy" else enemy2`. The sweep had
   checked dummy_* and enemy2_* prefixes and %Dummy/$Enemy2 scene
   paths, but never the BARE identifiers on their own.
   LESSON: when removing declarations, sweep for each removed name as a
   WHOLE WORD, with comments and string literals stripped first.

9. GUESSED FIELD NAMES, AGAIN (fourth time).
   _grant_mod used instance_id; a mod instance actually keys on
   mod_instance_id. Caught by the pre-export audit BEFORE delivery this
   time, which is the point of running it.

10. A NON-ASCII CHARACTER SURVIVED IN ability_drag_source.gd.
   An em-dash sat in a comment, violating the ASCII-only rule. Harmless
   in a comment, but it is exactly the character class the rule exists
   to catch. Cleaned.
